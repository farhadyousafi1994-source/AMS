; Site phones upload raw 5–20 MB photos. The app re-encodes every image down
; to under 1 MB on arrival (App\Support\CompressesImages), so the request has to
; be allowed in first — PHP's 2 MB default rejects it before any code runs.
upload_max_filesize = 25M
post_max_size = 26M
max_file_uploads = 20
